server:
  port: 8762
spring:
  application:
    name: service-hi

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

#logback
#logging:
#  config: classpath:logback-spring.xml